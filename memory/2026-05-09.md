# 2026-05-09 — Чт

## 📌 Ключевые события
- **llama-panel запущен** на production: порт 8081 (frontend), 8080 (backend + metrics)
- **Баги dashboard** исправлены: CPU NaN, GPU memory 0, Disk 16 KB
- **Пользовательские метрики** — реализована система (12 встроенных + кастомные)

## ⚡ Решения и действия
- Фронтенд на :3001 → переехал на :8081 (nginx/static)
- Backend на :8081 → переехал на :8081/api (systemd service)
- systemd services: `llama-8080.service`, `llama-panel.service` (enabled for boot)
- WebSocket live on :8081/ws — метрики в реальном времени

### Баги dashboard — фиксы
1. **CPU: NaN%** → `isFinite` проверка в `n()` валидаторе (Dashboard.jsx)
2. **GPU memory: 0** → `si.graphics()` не работает с NVIDIA, добавил `parseNvidiaSmi()` (system.js)
3. **Disk: 16 KB** → `fs === '/'` → ищем по `f.mount === '/'` (system.js)

### Пользовательские метрики
- Backend: `metrics-config.js` + `metrics-config.js` routes
- Frontend: `MetricsManager.jsx` + обновлённые Dashboard/App
- Config: `metrics-config.json` (создаётся автоматически)
- 12 встроенных метрик, 8 включено по умолчанию
- API: `/api/metrics-config` CRUD

## 🐛 Проблемы
- sudo пароль `XjiQmPeadrq888` записан в чат Telegram — нужно сменить
- Контекст сессии: были сбои каждые несколько сообщений
- Gateway restart → зацикливание ассистента

## 📋 Задачи на завтра
- [ ] Проверить метрики после всех фиксов
- [ ] Пересмотреть структуру daily-файлов (сейчас много дубликатов)

## 💡 Контекст
- Сервер: Ubuntu 24.04 LTS, Ryzen 5 5600XT, RTX 3090, 32 GB RAM
- llama-server работает с `--metrics`, порт 8080
- Метрики: CPU 1%, RAM 14%, GPU 85% VRAM, Disk 61% used
